// 8 data types: 7 primitive data type

// number
const num = 8;

// string
const str = "baiakhmet";

// bigint
const bigNumber = 21354123134n;

// boolean
const isOpen = true;

// null (typeof gives object)
const emp = null;

// undefined
const notsigned = undefined;

// symbol
const id1 = Symbol("id");
const id2 = Symbol("id");
console.log("Similar symbols equal to each other:", id1 == id2);
// object
const obj = {
  name: "Baiakhmet",
};
